# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# HTTP clients
httpx==0.27.0
requests==2.32.3

# Async DB
aiosqlite==0.20.0

# AI
# groq SDK powers the monitor / self-healing agents (analysis, prediction).
# The planner now runs the local fine-tuned model via Ollama (HTTP, uses
# `requests` above) — see planner_agent/model/README_OLLAMA.md.
# The assurance_agent verifies planner output: its structural layer is pure
# stdlib, its semantic layer reuses `requests` to call the BASE model
# qwen2.5:7b-instruct via Ollama (no LoRA) — see assurance_agent/README.md.
groq==0.9.0
urllib3>=1.26

# Azure (used by planner / executor)
azure-identity>=1.17.0
azure-mgmt-datafactory>=6.0.0
azure-storage-blob>=12.20.0

# File handling
python-multipart==0.0.9

# Machine learning — resource_agent + performance_prediction_agent load trained
# model bundles (.pkl) at runtime. joblib pickles are tied to the sklearn version
# that wrote them, so pin sklearn exactly and train (Kaggle notebook / run_training)
# against the SAME version, or model loading fails and the agents fall back to
# their heuristics. See resource_agent/training/README.md.
scikit-learn==1.7.0
joblib>=1.3.0
numpy>=1.26
pandas>=2.0
